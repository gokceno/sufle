# Sufle API Environment Variables
# Copy this file to .env and update with your settings

# Logging configuration
LOG_LEVEL=info
# Options: debug, info, warn, error

# Database configuration
DB_PATH=./db/db.sqlite
# Path to SQLite database file

# Database migrations
DB_MIGRATIONS_APPLY=true
# Automatically apply database migrations on startup
# Set to false in production if you want to run migrations manually

# Configuration file path
CONFIG_PATH=./sufle.yml
# Path to YAML configuration file

# Server configuration
# PORT=3000
# Host to bind to (default: 0.0.0.0)
# HOST=0.0.0.0

# Configuration overrides via environment variables
# You can override any YAML configuration using the format: PARENT__CHILD__VALUE

# Example: Override embedding API key
# EMBEDDINGS__OPTS__API_KEY=your-google-api-key-here

# Example: Override RAG retriever settings
# RAG__RETRIEVER__OPTS__K=10

# Example: Override output model API key
# OUTPUT_MODELS__0__CHAT__OPTS__API_KEY=your-google-api-key

# Example: Override tool configuration
# TOOLS__0__OPTS__API_KEY=your-weather-api-key

# Example: Override MCP server environment variables
# Note: These are passed to MCP servers, not Sufle itself
# MCP_DATABASE_URL=postgresql://localhost/db
# MCP_GITHUB_TOKEN=your-github-token

# Production considerations:
# - Set LOG_LEVEL=warn or LOG_LEVEL=error in production
# - Use absolute paths for DB_PATH and CONFIG_PATH
# - Set DB_MIGRATIONS_APPLY=false and run migrations separately
# - Use environment variables for all sensitive values (API keys, tokens)
# - Consider using a secrets management system for production
